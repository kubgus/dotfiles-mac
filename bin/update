#!/bin/sh
# Bring Homebrew and everything it manages up to date, then reclaim whatever
# the superseded versions were still holding on disk.
#
# No pipefail: this is /bin/sh, where it is not POSIX.
set -eu

brew update
brew upgrade
brew autoremove
brew cleanup
